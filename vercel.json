{
  "version": 2,
  "builds": [
    {
      "src": "frontend_react/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend_react/dist/$1"
    }
  ]
}
